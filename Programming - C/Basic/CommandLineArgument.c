#include<stdio.h>
#include<stdlib.h>
void main(int argn, char *argv[])
{
  int a;
  a=atoi(argv[1]);
  printf("%d",a);
}
// Use Alt+Shift+t or ctrl+` to open terminal in atom. Then use the normal gcc and a.exe commands.
